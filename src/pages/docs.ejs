<html>

<head>
    <link href="/static/styles/main.css" rel="stylesheet" type="text/css">
    <title>Documentation (v<%= version %>)</title>
</head>

<body>
    <section>
        <pre>NOTE FROM DEVELOPER!<br/><br/>These are the official APIs built solely for the <a href="https://sonyma.net">Sonyma</a> project. Third-parties are allowed to use them but without any warranty for longevity or legacy versions, they will not be upgraded or updated on request!</pre>
        <header>
            <ul>
                <li><a href="#">Movie API</a></li>
                <li><a href="#">TV API</a></li>
                <li><a href="#">Anime API</a></li>
            </ul>
        </header>
        <div class="docs">
            <select id="versionSelector">
                <% for (var i =1; i <= versions;  i++ ) { %>
                    <% if(version == i) {%>
                        <option selected value="<%= i %>">Version <%= i %></option>
                    <% } else { %>
                        <option value="<%= i %>">Version <%= i %></option>
                    <% } %>
                <% } %>
            </select>
            <%- injected_html %>
        </div>
    </section>
    <script>
        
        const vsel = document.getElementById('versionSelector');

        vsel.addEventListener('change', (e) => {
            document.location.search = '?version='+vsel.value;
        });
        
    </script>
</body>

</html>